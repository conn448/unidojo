name: Deploy Unidojo

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Make external SDK non-blocking
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          old = '<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script><script>'
          new = '<script async src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script><script>'
          if old not in s:
              raise SystemExit('Expected Supabase script tag not found')
          p.write_text(s.replace(old, new, 1), encoding='utf-8')
          print('Supabase SDK is now non-blocking')
          PY

      - name: Validate HTML and JavaScript structure
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          s = Path('index.html').read_text(encoding='utf-8')
          assert '<div id="app"></div>' in s
          assert 'function boot()' in s
          assert 'boot();' in s
          assert 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2' in s
          assert '</body></html>' in s
          print('Unidojo source validation passed')
          PY

      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload site
        uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
      - name: Smoke test live site
        run: |
          set -e
          URL="${{ steps.deployment.outputs.page_url }}"
          echo "Testing $URL"
          for i in 1 2 3 4 5 6; do
            if curl -fsSL --max-time 15 "$URL" -o /tmp/unidojo.html; then
              if grep -q "Unidojo" /tmp/unidojo.html && grep -q "id=\"app\"" /tmp/unidojo.html; then
                echo "LIVE SITE HTML OK"
                exit 0
              fi
            fi
            echo "Site not ready yet; retry $i"
            sleep 10
          done
          echo "LIVE SITE FAILED"
          exit 1
